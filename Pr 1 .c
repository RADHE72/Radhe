#include <stdio.h>

int main() {
    int sum = 0;

    // Calculate sum from 1 to 100
    for(int i = 1; i <= 100; i++) {
        sum += i;
    }

    // Display the result
    printf("The sum of numbers from 1 to 100 is: %d
